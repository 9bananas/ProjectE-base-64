WIP - DO NOT USE

-------------------------------

trying to adjust all basline values for EMC to use a base value of 64 instead of 1.

Minecraft crafting station only support 1 slot of output, with a maximum amount of 64 items.
So, if the minimum EMC of base items that everything else inherits it's values from is at least 64, it should solve most downstream issues of EMC generation!

at least that's the theory...
